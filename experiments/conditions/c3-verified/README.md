# C3 verifier gate

C3 reuses the exact C2 assembly representation and generation prompt. Comparative C3 runs are disabled until a separately reviewed verifier policy is versioned and frozen.

Current refusal code: `C3_VERIFIER_NOT_FROZEN`.

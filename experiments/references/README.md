# Reference inputs

Target references are deliberately absent. Before a run, each externally approved reference must be registered with an `asset_id`, filename, and SHA-256. The runner recomputes the file digest and records it in the immutable run manifest.

Do not add Pikachu, Magikarp, Psyduck, Magnemite, or other target references without explicit approval.
